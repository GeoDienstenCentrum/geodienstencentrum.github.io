language: ruby

cache: bundler

gemfile: travisGemfile

rvm:
  - 2.1

install: bundle install

script: jekyll build && htmlproof ./_site && scss-lint ./_sass/

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer
